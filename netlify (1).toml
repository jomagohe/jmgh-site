[build]
  command = "bundle config set --local path 'vendor/bundle' && bundle install --jobs=4 --retry=3 && bundle exec jekyll build"
  publish = "_site"

[build.environment]
  RUBY_VERSION = "3.2.0"
